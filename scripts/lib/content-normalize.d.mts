/** Type declarations for `content-normalize.mjs` so TypeScript tests can import it. */
export declare function normalizeForMatch(input: string): string;
export declare function quoteAppearsIn(quote: string, normalizedSource: string): boolean;
